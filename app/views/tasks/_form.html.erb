<%= form_for @task do |f| %>
<% if @task.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@task.errors.count, "error") %> prohibited
    this task from being saved:</h2>
    <ul>
      <% @task.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
</div>
<% end %>
<div>
  <p>
  <%= f.label :task %>
  </p>
  <p>
  <%= f.text_field :task %>
  </p>

  <p>
  <%= f.label :due %> (YYYY-MM-DD)
  </p>
  <p>
  <%= f.text_field :due %>  
  </p>
  <p>
  <%= f.submit %>
  </p>
</div>  
<% end %>